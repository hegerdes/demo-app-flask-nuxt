apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "flask-frontend.fullname" . }}
  labels:
    app: {{ template "flask-frontend.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
data:
  env: dev
